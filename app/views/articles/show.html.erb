<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Are you sure want to delete this article?"} %>

<%= link_to "Home", articles_path() %>